{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Server",
      "type": "shell",
      "command": "python -m http.server 8080",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "^.*Serving HTTP.*$",
          "endsPattern": "^.*Serving HTTP.*$"
        }
      }
    }
  ]
}
